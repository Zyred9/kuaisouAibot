bots:
  config:
    logs: false
    enable-proxy: false
    background-group-id: 123456789001
    notify-chat-id: -1003319507819
    tokens:
      BOT_TOKEN: 7625159298:AAFGjgPTBlPtc2TaNfFo7Ph2FAZgVjK9Rno
    qr-code-path: /home/bots/search/images
    history-url: http://localhost:8813/collect/td/history
    recycling: true
# -------------------部署需要修改---------------------

mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: true

  global-config:
    # 全局配置
    banner: false
    db-config:
      table-prefix: t_
      # 数据库配置
      id-type: auto
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Qj683k2^11299%nkjb
    url: jdbc:mysql://127.0.0.1:3306/search_engin?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Shanghai&useSSL=false&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 50           # 最大连接数50个
      minimum-idle: 10                # 最小空闲连接10个
      connection-timeout: 30000       # 连接超时30秒
      idle-timeout: 600000            # 空闲连接超时10分钟
      max-lifetime: 1800000           # 连接最大存活30分钟
      connection-test-query: SELECT 1  # 连接测试查询
      leak-detection-threshold: 60000  # 连接泄漏检测60秒
      pool-name: BotHikariPool        # 连接池名称
      initialization-fail-timeout: -1  # 初始化失败不阻塞启动
  elasticsearch:
    uris:
      - http://localhost:9200
    username: elastic
    password: umEjOIAMYkf3vPq9YEaB
    connection-timeout: 5000
    socket-timeout: 60000
  data:
    elasticsearch:
      repositories:
        enabled: true
  redis:
    host: localhost
    port: 6379
    database: 0
    password: Qj683k2^11299%nkjb
    # Lettuce 连接池配置
    lettuce:
      pool:
        max-active: 50      # 最大活动连接数
        max-idle: 20        # 最大空闲连接数
        min-idle: 5         # 最小空闲连接数
        max-wait: 3000      # 最大等待时间(毫秒)
logging:
  level:
    com.db.bots: ERROR